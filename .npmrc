engine-strict=true
# Without this, `npm install` or `yarn` in this repo succeeds and silently writes a
# second lockfile — the corepack policy documented in CLAUDE.md becomes prose.
package-manager-strict=true
