engine-strict=true
# pnpm-only: refuses to run when the version does not match `packageManager`. npm ignores it
# ("Unknown project config"), so on its own it does not stop an `npm install` — what stops
# `yarn` is corepack shimming it, not this key.
package-manager-strict=true
