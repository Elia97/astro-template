// Minimal stub of astro:content (aliased in vitest.config.ts). Tests that need
// specific collection data replace this via vi.mock / vi.mocked.
export async function getCollection(): Promise<unknown[]> {
  return []
}
