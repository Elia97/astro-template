// Mirrors astro.config.mjs's i18n block for unit tests — astro:config/client
// is a virtual module that only exists inside the Astro runtime.
export const i18n = {
  defaultLocale: 'it',
  locales: ['it'],
}
